<h1>Users::Items#index</h1>

	<div class="col-xs-9">
		<h2>Items index</h2>
		<!--books一覧 -->
		<table class="table table-hover table-inverse">
			<thead>
				<tr>
					<th></th>
					<th>Title</th>
					<th>Opinion</th>
					<th></th>
					<th></th>
				</tr>
			</thead>

			<tbody>
				<% @items.each do |item| %>
				<tr>
					<td>
						<%= link_to users_user_path(item.user) do %>
						<%= attachment_image_tag(item.user, :user_image, :fill, 50, 50, fallback: "no-image-mini.jpg") %>
						<% end %>

					</td>
					<td><%= link_to item.title %></td>
					<td><%= item.description %></td>

					<%#<% if item.favorited_by?(current_user) %>
					<td>
						<%#<%= link_to item_favorites_path(item), method: :delete do %>
						<%#<i class="fas fa-hamburger" aria-hidden="true" style="color: orange;"></i>
				        <%= item.favorites.count %>
				        <%#<% end %>
					</td>
					<%#<% else %>
					<td>
						<%#<%= link_to item_favorites_path(item), method: :post do %>
						<%#<i class="fas fa-hamburger" aria-hidden="true" style="color: grey;"></i>
					      <%= item.favorites.count %>
					    <%#<% end %>
					</td>
					<%#<% end %>
					<td>コメント数：<%#<%= item.item_comments.count %></td>
				</tr>
				<% end %>
			</tbody>
		</table>

	</div>
