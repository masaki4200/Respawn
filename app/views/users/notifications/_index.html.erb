 <!-- 自分の投稿に対するいいね、コメントは通知に表示しない -->

<!-- <div class="container notification"> -->

<%# if notifications.exists? %>
  <%#= render notifications  %>
  <%#= paginate notifications %>
<%# else %>
	<!-- <p>通知はありません</p> -->
<%# end %>

<!-- </div> -->
<!-- モーダル・ダイアログ -->
<% if user_signed_in? %>
<a type="button" class="btn btn-lg" data-toggle="modal" data-target="#sampleModal">
	<% if unchecked_notifications.any?%>
	<i class="far fa-bell true" ></i>
	<% else %>
	    <i class="far fa-bell false" ></i>
    <% end %>
</a>
<!-- <button type="button" class="btn btn-primary btn-lg" data-toggle="modal" data-target="#sampleModal">
  通知
</button>
 -->
<div class="modal fade" id="sampleModal" tabindex="-1">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal"><span>×</span></button>
        <h4 class="modal-title">通知一覧</h4>
      </div>
      <div class="modal-body">
      	<%# binding.pry %>
        <% if notifications.exists? %>
			<%= render notifications  %>
		  	<%= paginate notifications %>
		<% else %>
			<p>通知はありません</p>
		<% end %>
      </div>
      <div class="modal-footer">
      </div>
    </div>
  </div>
</div>
<% end %>
