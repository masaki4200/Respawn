


<div class="container">
  <div class="row">

		<div class="col-xs-10 col-xs-offset-1">
			<h1>ユーザー　一覧</h1>
			<h2>Users</h2>
			<!--users一覧 -->
			<table class="table">
				<thead>
					<tr>
						<th>Image</th>
						<th>Name</th>
						<th>Item count</th>
						<th>Follows</th>
						<th>Followers</th>
					</tr>
				</thead>
				<tbody>
					<% @users.each do |user| %>
					<tr>
						<td><%= attachment_image_tag(user, :user_image, size: "50x50", fallback: "noimage2.png") %></td>
						<td><%= user.name%></td>
						<td>件</td>

						<td><%#<%= user.followings.count %>人</td>
						<td><%#<%= user.followers.count %>人</td>



						<td><% if current_user != user %>
						        <%#<% if user.followed_by?(current_user) %>
						            <p><%#<%=link_to "フォロー済み", user_relationships_path(user.id), method: :delete, class: "btn btn-success col-xs-7"%></p>
						        <%#<% else %>
						            <p><%#<%=link_to "フォローする", user_relationships_path(user.id), method: :post, class: "btn btn-default col-xs-7" %></p>
						        <%#<% end %>
						    <% end %>
						</td>
						<td><%#<%= link_to "Show", user, class: "user_#{user.id}" %></td>
					</tr>
					<% end %>
				</tbody>
			</table>
			<!--users一覧 -->
		</div>
  </div>
</div>